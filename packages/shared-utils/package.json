{
  "name": "@mixer/shared-utils",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utility functions for Mixer monorepo",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf .turbo"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}