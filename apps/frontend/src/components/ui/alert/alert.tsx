import * as React from 'react';import { AlertCircle, CheckCircle, Info, X } from 'lucide-react';import { Alert as MantineAlert } from '@mantine/core';export interface AlertProps extends React.HTMLAttributes<HTMLDivElement> { variant?: 'info' | 'success' | 'warning' | 'error'; title?: string; children: React.ReactNode; onClose?: () => void;}const iconMap = { info: Info, success: CheckCircle, warning: AlertCircle, error: AlertCircle,};const colorMap = { info: 'blue', success: 'green', warning: 'yellow', error: 'red',};const Alert = React.forwardRef<HTMLDivElement, AlertProps>(({ variant = 'info', title, children, onClose, ...props }, ref) => { const Icon = iconMap[variant]; const color = colorMap[variant]; return ( <MantineAlert ref={ref} color={color} title={title} onClose={onClose} {...props}> {children} </MantineAlert> );});Alert.displayName = 'Alert';export { Alert };
